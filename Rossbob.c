/* Include le ficier header (*.h) */
#include "rossbob.h"

/* Le fonction defini dans le header */
int feedcat (int foodstate, int rbappear)
{
	if (foodstate < FOODREQ/4 && rbappear = 1)
	{
		return 1;
	}
	else
	{
		return 0;
	}
}
