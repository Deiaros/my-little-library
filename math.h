#define addition(x, y) ((x) + (y))
#define soustraction(x, y) ((x) - (y))
#define multiplication(x, y) ((x) * (y))
#define division(x, y) ((x) / (y) && (y) != 0)
#define comp(x, y) ((x) == (y))
#define sqr(x) ((x) * (x))

int division(int x, int y) 
